# CrachoLM Dependencies
# =====================
# For GPU (CUDA) support on Linux with PyTorch, run:
# pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
